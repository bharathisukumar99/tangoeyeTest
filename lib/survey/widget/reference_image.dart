import 'package:flutter/material.dart';

class ReferenceImage extends StatelessWidget {
  const ReferenceImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
