enum FetchStatus {
  initial,
  loading,
  success,
  failure,
}

enum AnswerType {
  image,
  multiplechoicemultiple,
  multiplechoicesingle,
  yesNo,
  descriptive,
  empty
}

enum OverallValidation { initial, failure, success }
